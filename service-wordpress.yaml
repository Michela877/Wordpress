apiVersion: v1
kind: Service
metadata:
  name: wordpress-service
spec:
  type: LoadBalancer
  loadBalancerIP: <IL_TUO_IP_STATICO> # Sostituisci con il tuo IP pubblico
  selector:
    app: wordpress
  ports:
    - name: http
      port: 8080
      targetPort: 8080
